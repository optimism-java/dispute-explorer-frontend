import { Pager, SearchParams } from "./common";

import { Game, ClaimData, Credit, CreditDetail } from "./data";

export type Size = "2xl" | "xl" | "lg" | "md" | "sm" | "xs";

export type { Pager, SearchParams, Game, ClaimData, Credit, CreditDetail };
