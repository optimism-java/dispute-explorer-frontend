import { Box } from '@mui/material';

const Header = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <span className="font-dogica">Dispute Game</span>
    </Box>
  );
};

export default Header;
